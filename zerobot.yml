---
properties:

  - name: db_name
    default: rails
    description: Name of the database to create

  - name: db_username
    default: postgres
    description: The name of the user who will own the database

  - name: db_password
    description: The password for the database user
